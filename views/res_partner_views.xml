<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_partner_subscription_button" model="ir.ui.view">
        <field name="name">res.partner.subscription.button</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <div name="button_box" position="inside">
                <field name="subscription_count" invisible="1"/>
                <button name="action_view_subscriptions" type="object"
                        class="oe_stat_button" icon="fa-refresh"
                        invisible="subscription_count == 0">
                    <div class="o_field_widget o_stat_info">
                        <span class="o_stat_value">
                            <field name="active_subscription_count"/>
                        </span>
                        <span class="o_stat_text">Subscriptions</span>
                    </div>
                </button>
            </div>
        </field>
    </record>
</odoo>
